class ScannerRepository {

    suspend fun analyze(url: String): List<Vulnerability> {
        // Solicitud HTTP
        // Obtención de headers
        // Llamada al HeaderAnalyzer
        return emptyList()
    }

}
